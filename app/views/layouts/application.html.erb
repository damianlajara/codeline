<!DOCTYPE html>
<html>
<head>
  <title>Codeline</title>
  <!-- Used to md5 email hash in order to get gravatar tag in react-->
  <%= javascript_include_tag 'http://crypto-js.googlecode.com/svn/tags/3.1.2/build/rollups/md5.js' %>
  
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

  <meta content='<%= user_signed_in? ? current_user.id : "" %>' name='user-id'/>
  <%= csrf_meta_tags %>
</head>
<body>
  <%= render 'layouts/header' %>
  <%= render 'layouts/sidebar' %>
    <div id="main">
      <div class="container-fluid">
        <div class="row">
          <div class="col-md-12 reset">
            <% if user_signed_in? %>
              <%= link_to "#", class: "toggle-sidebar", "data-toggle" => "offcanvas" do %>
                <%= fa_icon "navicon" %>
              <% end %>
            <% end %>
            <%= bootstrap_flash class: "text-center" %>
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
  <% if user_signed_in? %>
  </div> <!-- End sidebar -->
  <% end %>
</body>
</html>
